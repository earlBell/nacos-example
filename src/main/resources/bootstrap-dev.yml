#nacos要求配置注册必须放在bootstrap层次
spring:
  cloud:
     nacos:
        config:
           server-addr: localhost:8848
           # 指定自定义namespace的id
           namespace: 3146d3eb-2422-4439-a063-a9a0df197c5e
           group: GZ
           # 指定配置文件后缀,nacos要求（组成dataId）
           file-extension: yml